# `public/assets/`

**Token list:** optional immutable `logoURI` values belong in `public/tokenlists/aegis.tokenlist.json` (e.g. Arweave/IPFS URLs). This folder intentionally has **no bundled logo images** for the dApp chrome.

**Circuits / config:** live alongside this directory under `public/`; they are not raster images.
